<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.planner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewOfferedTasksActivity"></activity>
        <activity
            android:name=".SignInActivity"
            android:label="SignInActivity" />
        <activity android:name=".SetTaskCompletedActivity" />
        <activity
            android:name=".FriendActivity"
            android:label="@string/title_activity_friend"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewWishesActivity"
            android:label="@string/wishes" />
        <activity
            android:name=".ViewTasksActivity"
            android:label="@string/tasks" />
        <activity
            android:name=".NewWishActivity"
            android:label="@string/add_new_wish" />
        <activity android:name=".SetWishCompletedActivity" />
        <activity
            android:name=".NewTaskActivity"
            android:label="@string/add_new_task"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>